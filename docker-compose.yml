services:
  app:
    build: .
    container_name: photo-albums
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=file:/app/data/db.sqlite
    volumes:
      - ./data:/app/data
      - ./public/albums:/app/public/albums
